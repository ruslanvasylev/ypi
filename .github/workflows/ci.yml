name: CI

# Hermetic PR/push gate — the ONLY automated check that runs on inbound PRs
# (local git hooks never run on a contributor's machine). Deliberately:
#   * NO real-LLM e2e (those run only from an explicit local invocation),
#   * pi PINNED to .pi-version, never "latest", so an upstream pi release can never
#     redden this gate. Upstream drift is a re-pin signal handled locally, not in CI.
# Green here = a fresh clean-room repro of the hermetic gates.

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true

      - name: Fetch pinned Pi source tag
        shell: bash
        run: |
          PI_VERSION="$(tr -d '[:space:]' < .pi-version)"
          PI_TAG="v${PI_VERSION}"
          git -C pi-mono fetch --force --depth=1 origin \
            "refs/tags/${PI_TAG}:refs/tags/${PI_TAG}"
          test "$(git -C pi-mono rev-parse "${PI_TAG}^{commit}")" \
            = "$(git -C pi-mono rev-parse HEAD)"
      - uses: oven-sh/setup-bun@v2

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Install Pi (pinned to .pi-version, never "latest")
        run: bun install -g "@earendil-works/pi-coding-agent@$(tr -d '[:space:]' < .pi-version)"

      - name: Show versions
        run: |
          echo "pi: $(pi --version)"
          echo "bun: $(bun --version)"

      - name: Hermetic gate (test-fast + extensions; no LLM)
        run: scripts/pre-push-checks
